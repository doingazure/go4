name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  # # avoid "Browserslist: caniuse-lite is outdated" warning
  # update_caniuse_db:
  #   if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Update caniuse-lite db
  #       run: |
  #         # Add a new step to upload npm logs as an artifact
  #         - name: Upload npm logs
  #           uses: actions/upload-artifact@v2
  #           with:
  #             name: npm-logs
  #             path: /home/runner/.npm/_logs
  #         echo "PRE version checkups"
  #         npx -v
  #         npm -v
  #         echo "POST version checkups"
  #         npm install caniuse-lite@latest
  #         echo "STEP 10"
  #         npx caniuse-lite@latest --update-db
  #         echo "STEP 20"
 
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: ↪︎ Get Oriented
        run: |
          echo "↪︎ version checkups"
          echo "⇨ version checkups"
          echo "▶ version checkups"
          echo "pwd:            $(pwd)"
          echo "npx version:    $(npx -v)"
          echo "npm version:    $(npm -v)"
          echo "node version:   $(node -v)"
          echo "Nextjs version: $(npx next -v)"
          echo "✅ version checkups"

          echo "echos... 🌱 🐣 🌅 🏁 ▶️"

          echo "▶️ Nextjs telemetry"
          # Nextjs collects telemetry - just raising awareness
          # npx next telemetry disable
          # npx next telemetry enable
          npx next telemetry status
          echo "✅ Nextjs telemetry"

          # DEBCONF_NOWARNINGS=yes
          # echo "DEBCONF_NOWARNINGS=$DEBCONF_NOWARNINGS"

      - name: Update browserslist
        run: |
          echo "↪︎ browserslist update"
          # npm install caniuse-lite@latest # is this needed?
          npx update-browserslist-db@latest
          echo "✅ browserslist update"

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_BAY_0319D780F }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/" # App source code path
          api_location: "/api" # Api source code path - optional
          output_location: "out" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_BAY_0319D780F }}
          action: "close"
